fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.1

# AI/ML
langchain==0.2.6
langchain-community==0.2.6
llama-index==0.10.50

huggingface-hub==0.23.4

# CPU-only PyTorch build -- this stack has no GPU anywhere (Ollama and
# sentence-transformers both run CPU inference here), but the default
# PyPI torch wheel pulls in ~2GB of unused NVIDIA CUDA libraries
# (nvidia-cublas, nvidia-cudnn, nvidia-cufft, etc.) as transitive
# dependencies of sentence-transformers below. Pinning the +cpu build
# explicitly (via PyTorch's own CPU wheel index) avoids downloading any
# of that.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu

# Local (offline-capable) embedding model runner -- SentenceTransformer (intfloat/multilingual-e5-large)
# and fastembed (ONNX). See app/utils/embeddings.py.
sentence-transformers>=3.0.0
fastembed==0.8.0

# Database clients
asyncpg==0.29.0
neo4j==5.20.0
redis[hiredis]==5.0.7
pgvector==0.3.0

# Async task queue
celery==5.4.0
redis==5.0.7

# HTTP
httpx==0.27.0

# Parsing
pymupdf==1.24.5
python-docx==1.1.2

# Utils
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
structlog==24.2.0
opentelemetry-sdk==1.25.0
opentelemetry-exporter-otlp==1.25.0
prometheus-client==0.20.0
